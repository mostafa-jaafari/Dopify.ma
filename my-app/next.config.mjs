/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'img.freepik.com', 's.gravatar.com', 'res.cloudinary.com']
    }
};

export default nextConfig;
